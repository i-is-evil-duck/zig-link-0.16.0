services:
  zig-link:
    build: .
    ports:
      - "8081:8080"
    volumes:
      - zig-link-data:/app/data
    restart: unless-stopped
    environment:
      - PORT=8080

volumes:
  zig-link-data: